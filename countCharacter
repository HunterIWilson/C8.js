function countCharacter(str, char) {
  // your code here 
  var count = 0
  for (var x in str){
    if (str[x] == char){
      count += 1
    }
    
  }
  return count;
    
    
  }

countCharacter('I am a hacker', 'a');
